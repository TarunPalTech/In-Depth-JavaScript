/* 
| Feature                | splice()         | slice()   |
| ---------------------- | ---------------- | --------- |
| Changes original array | ✅ Yes           | ❌ No   |
| Can add elements       | ✅ Yes           | ❌ No   |
| Can remove elements    | ✅ Yes           | ❌ No   |
| Returns                | Removed elements |New array |

splice() is just like a surgery (array is modified)
slice() is just like a photocopy (array stays safe)


🎯 Interview Line

splice() modifies the original array by adding, removing, or replacing elements and returns the removed elements.

*/